{
  "devDependencies": {
    "@wordpress/env": "^8.4.0"
  },
  "scripts": {
    "phpunit": "wp-env start && wp-env run --env-cwd=\"wp-content/plugins/vk-css-optimize\" tests-wordpress vendor/bin/phpunit -c .phpunit.xml.dist --verbose"
  }
}
